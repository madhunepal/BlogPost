<h2><%= @message.title %></h2>
<p><%= @message.description %></p>

<%= link_to "Back", root_path %>
<%= link_to "Delete", message_path(@message), method: :delete, data: {confirm: "Are you sure to delete this?"} %>

<%= link_to "Edit", edit_message_path(@message) %>